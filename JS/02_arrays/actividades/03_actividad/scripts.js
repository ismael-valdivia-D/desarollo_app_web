function diferenciaCentroExtremos(){
    console.log("hola")
    let numeros = [5,6,9,5,2];
    let sum = numeros[0] + numeros[numeros .length -1];
    let medio= numeros[2]
    let resta= medio - sum
    alert("La diferencia entre centros y extremos es: \n" 
        +resta );
}